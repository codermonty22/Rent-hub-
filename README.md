# Rent-hub-
Fullstack e-commerce renting platform built with Flask, MongoDB, and JavaScript. Features user auth with 2FA, product listings, admin dashboard, cart, reviews, and real-time chat . Tech: Flask (Python), MongoDB, JWT, HTML/CSS/JS.  Setup: Clone, pip install -r requirements.txt, configure MongoDB, run python backend/app.py
